	
 	var text =document.getElementById('text')
	var typing=document.getElementById('typing')

	
	function pupper(){
	typing.innerHTML = text.value;
}
	
	//.bind() can bind a function to a certain key
	//.val() works with input data that we input throught input tags
	//.keydown() means when i press this key down
	//.keyup() means when I let up on a key it will do this
	//keyCodes refer to the keyboard with numbers




// function checkSubmit(text) {
//    if(e && e.keyCode == 13) {
//      ;
//    }
// }
